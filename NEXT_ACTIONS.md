# Next Actions

## Do Next

- [ ] Run a small ignored-vault smoke test of the new versioning UX in `test_vaults/Memex`: `version status`, one bounded dry-run-free write, `version log`, `version changed-files <run-id>`, and a path-level restore on a disposable test note.
- [ ] Review the converted pending proposal `model-block-01-inbox-new-images-md-classify-type.json`; approve/apply only through `review-proposals` if `type: source` is accepted for `01 Inbox/New Images.md`.
- [ ] Leave the remaining low-confidence model block for `01 Inbox/Untitled 1 copy.md` pending unless a human review accepts `type: note`; `review-model-blocks --approve-safe` now skips it because confidence `0.65` is below the configured `0.75` threshold.
- [ ] If Obsidian is open on the Memex test vault, run `obsidian-check --live-obsidian`; otherwise keep using `obsidian-check --json` as the static acceptance gate.
- [ ] Review `test_vaults/Memex/00 System/0.01 agent/review/proposals/base-hierarchy.json`; if the dashboard paths and generated coverage summaries look useful, approve and apply it in the ignored Memex test vault only.
- [ ] Run a transcript-file schema onboarding rehearsal against a copied/test vault conversation file, then review the generated `schema-conversation-summary.md` and pending proposal JSON.
- [ ] Run an edited `export-schema-defaults` / `import-schema-defaults` rehearsal against a copied/test vault, approve only the generated pending proposal, then write a fresh norms lock.
- [ ] Add a small report comparison helper for Memex pilots so validation counts, stale tracked notes, blocked stages, and cleanup queue totals can be compared across runs.

## Then

- [ ] Add more targeted schema-change proposal operations for deprecating or migrating controlled values.
- [ ] Use the base hierarchy's `Needs metadata` count to prioritize bounded cleanup and property-value passes before broad dashboard rollout.

## Completed

- [x] Implement the dashboard-first default layout. Evidence: fresh vaults create `00 Inbox`, `01 Dashboards`, People/Organizations/Work/Administrative/Thoughts/Sources branches, and `99 System`; `propose-inbox-sort` and `propose-vault-layout` preserve review/versioning boundaries; clean external smoke validation reports 0 issues and Obsidian check reports 0 errors/0 warnings.
- [x] Add editable Markdown schema-default export/import. Evidence: `vault-agent init` creates `0.024 vault defaults.md`; `export-schema-defaults` writes a portable Markdown contract; `import-schema-defaults` validates edited Markdown and writes only a pending `vault-schema-defaults.json` proposal; malformed imports write no proposal; verified with `python3 -m unittest discover -s tests` running 208 tests and `npm run check`.
- [x] Streamline vault startup and onboarding. Evidence: normal launches continue the latest vault-local session unless explicitly overridden; first launch offers one default/custom folder choice, initializes and scans, then starts onboarding; `vault_status` reports schema lifecycle, prior scan, inbox deltas, issue groups, pending review, and prior reports; startup assessment remains read-only.
- [x] Add bidirectional pi-forge/pi-vault MCP integration. Evidence: pi-vault exposes sequential forge client tools and `vault_submit_artifact`; `pi-vault-mcp` exposes only status and pending artifact submission; pi-forge includes the reverse client and `vault-handoff` skill; 172 Python tests and both full checks pass; isolated installed launchers completed live sequential local-model handoffs, pending-only verification, explicit temporary-vault review/apply, Git change-set inspection, and a clean static Obsidian check.

- [x] Integrate the engine into pi-vault. Evidence: added `.pi-vault/config.yaml` bootstrap paths, parameterized runtime state, bundled pi extension and seven skills, namespaced automation commands, installer, validated move/rename operations, and custom-path/move regressions; verified with 157 Python tests and the pi extension test.

- [x] Add Git-backed versioning as a first-class safety layer. Evidence: added `vault_agent/versioning.py`, `vault_agent/execution.py`, `vault_agent/version_cli.py`, versioning config defaults, managed `.gitignore` support, pre/post snapshots, change-set JSONL, rollback commands, mass-edit gating, `hermes-run` vault-level wrapping, docs, starter guidance, and `tests/test_versioning.py`; verified with `python3 -m unittest discover -s tests` running 143 tests.
- [x] Prove the Memex model-block review loop and tighten `--approve-safe`. Evidence: `autonomous-run --create-lock --use-llm --stage classify-type --max-notes 2` produced version run id `95cbdb5a594a4118a2ebcd94b614b7dc`, wrote autonomous report `autonomous-run-20260618-231325`, and produced 2 blocked model proposals; `review-model-blocks --dry-run --approve-safe` reported 1 safe conversion and 1 unsafe skip; `review-model-blocks --approve-safe` created the pending `model-block-01-inbox-new-images-md-classify-type.json` proposal and left the low-confidence `Untitled 1 copy.md` block pending; `review-proposals --dry-run` validated 11 proposals with 0 invalid and no note mutation; `obsidian-check --json` remained at 0 errors and 22 warnings; verified with `python3 -m unittest discover -s tests` running 153 tests.
- [x] Make blocked model stages reviewable instead of only blocked. Evidence: added `vault-agent review-model-blocks`, `model-blocked-proposals.json` / `.md`, model-block summaries in organization/autonomous reports, conversion of safe blocked stage proposals into normal pending review proposal JSON, and tests covering dry-run, conversion, validation, and no direct note mutation; verified with `python3 -m unittest discover -s tests` running 152 tests.
- [x] Add bounded autonomous scheduled maintenance, schema-conversation proposal generation, and Obsidian static compatibility checks. Evidence: added `vault-agent autonomous-run`, updated `hermes-run` to use the same autonomous routine, added `vault-agent schema-conversation --conversation-file`, added `vault-agent obsidian-check`, fixed nested-vault Git detection, and verified with `python3 -m unittest discover -s tests` running 150 tests plus Memex dry-run/static/live LLM smoke checks.
- [x] Run the first live LLM-backed autonomous Memex pilot without auto-applying existing safe proposals. Evidence: `autonomous-run --create-lock --use-llm --stage classify-type --max-notes 2` produced version run id `91996b5e6f2744719a30fb32eb3c7e69`, wrote autonomous report `autonomous-run-20260618-224639`, processed 2 classify stages, changed 1 note, and blocked 1 near-threshold/warning-bearing proposal for review.
- [x] Run the first report-driven Memex pilot and first bounded real organization pass. Evidence: wrote `test_vaults/Memex/00 System/0.01 agent/norms-lock.json` with lock hash `32d9e490686c9b55aa07a7c954d7223a79ef6d90035b61e3970bbdca6fede34a`; refreshed proposal review output to current; generated `cleanup-queue-vault.json` with 5 `update_frontmatter` operations and `"remove": []`; approved and applied only that cleanup proposal; ran `organize-vault-pass --note "01 Inbox/Important Concepts.md" --stage property-values --max-notes 1 --use-llm`; report `test_vaults/Memex/00 System/0.01 agent/reports/organization-run-20260617-224737.md` recorded 1 processed stage, 1 changed note, 0 blocked stages, and the active lock hash; rebuilt retrieval; verified with `python3 -m unittest discover -s tests` running 129 tests.
- [x] Build higher-level Bases/dashboard proposal helpers from sparse metadata. Evidence: added `vault-agent propose-base-hierarchy`, `base-hierarchy` proposal validation, deterministic domain/parent grouping, embedded Base dashboard rendering, optional LLM coverage wording fallback, docs, and Memex proposal smoke validation; verified with `python3 -m unittest discover -s tests` running 135 tests.
- [x] Make larger LLM-backed organization batches serialized and auditable. Evidence: `organize-vault-pass` now reports `LLM prompts: serialized one note stage at a time`, writes `llm_prompts_serialized` and per-item `queue_position` in organization reports, and a regression test proves two LLM `property-values` calls are made in queue order without overlap; verified with `python3 -m unittest discover -s tests` running 130 tests.
- [x] Add locked vault norms and full-pass reporting infrastructure. Evidence: added `vault-agent norms-lock`, lock-aware processing ledger behavior, `vault-agent organize-vault-pass`, report artifacts under `00 System/0.01 agent/reports/`, and status/report docs; verified with `python3 -m unittest discover -s tests` running 126 tests.
- [x] Add a queue helper that generates cleanup proposals from validation groups in bounded batches. Evidence: added `vault-agent propose-cleanup-queue` with `--folder`, `--max-items`, `--remove-unknown`, and `--overwrite-proposal`; generated proposals validate through `review-proposals --dry-run`; verified with `python3 -m unittest discover -s tests` running 126 tests.
- [x] Add readiness-first organization preflight and generated-state validation. Evidence: added `vault-agent organization-readiness`, `validate --json`, generated-state/template checks, `status` readiness output, and `reconcile --dry-run` preflight sections; verified with `python3 -m unittest discover -s tests` running 129 tests.
- [x] Improve local LLM JSON repair behavior for staged organization commands. Evidence: `OpenAICompatibleProposalProvider` now uses one shared JSON-object extraction/repair/failure pipeline for full and stage-specific proposals; verified with provider retry/failure tests and `python3 -m unittest discover -s tests` running 129 tests.

- [x] Create the initial Python CLI skeleton and package layout for `vault-agent` with help/no-op command routing only. Evidence: added `pyproject.toml`, `vault_agent/`, and `tests/test_cli.py`; verified with `python3 -m vault_agent --help`, `python3 -m vault_agent scan`, `python3 -m vault_agent memory --help`, and `python3 -m unittest discover -s tests`.
- [x] Add shared config loading for vault root, dry-run, and verbose options. Evidence: added `vault_agent/config.py`, wired config into `vault_agent/cli.py`, expanded `tests/test_cli.py`, and verified with `python3 -m unittest discover -s tests`, `python3 -m vault_agent --help`, `python3 -m vault_agent scan --dry-run`, and `python3 -m vault_agent --verbose scan`.
- [x] Implement `vault-agent init --dry-run` to report the agent folder and starter files that would be created. Evidence: added `vault_agent/init.py`, routed `init --dry-run` through `vault_agent/cli.py`, expanded `tests/test_cli.py`, and verified with `python3 -m unittest discover -s tests`, `python3 -m vault_agent init --dry-run`, and `python3 -m vault_agent --vault-root /tmp/example-vault init --dry-run`.
- [x] Implement safe folder/file creation helpers with no-overwrite behavior and backup planning. Evidence: added `vault_agent/safety.py`, integrated creation planning into `vault-agent init --dry-run`, added `tests/test_safety.py`, and verified with `python3 -m unittest discover -s tests` and `python3 -m vault_agent init --dry-run`.
- [x] Define the initial `schema.json` structure and starter note types/properties. Evidence: added `vault_agent/schema.py` and starter schema/template rendering; verified with `python3 -m unittest discover -s tests`.
- [x] Establish the default note metadata schema as `type`, `status`, `domain`, `parent`, `related`, `cover`, `source_kind`, and `capture_type`. Evidence: updated `vault_agent/schema.py`, templates, scanner, validation, reconcile, and proposal processing; verified with `python3 -m unittest discover -s tests`.
- [x] Implement non-dry-run `vault-agent init` using safe creation helpers. Evidence: wired `apply_init` through `vault_agent/cli.py`; verified starter files in `tests/test_cli.py` and `python3 -m unittest discover -s tests`.
- [x] Add starter file contents for config, state, manifest, review, retrieval, and human-readable schema files. Evidence: added `vault_agent/starter_files.py`; verified with init tests.
- [x] Add deterministic scan, manifest/state, vault map, and note catalog generation. Evidence: added `vault_agent/scanner.py`; verified with workflow tests.
- [x] Add validation review queues. Evidence: added `vault_agent/validation.py`; verified malformed/unknown metadata workflow test.
- [x] Add safe writes, backups, and command logs. Evidence: expanded `vault_agent/safety.py`, added `vault_agent/logging_utils.py`, and verified backup creation in workflow tests.
- [x] Add inbox-only processing and retrieval rebuild. Evidence: added `vault_agent/processor.py` and `vault_agent/retrieval.py`; verified workflow tests.
- [x] Add Hermes scheduled-run command. Evidence: added `vault_agent/hermes.py` and `hermes-run`; verified dry-run workflow test.
- [x] Define and validate the LLM JSON proposal schema before allowing notes to become `processed`. Evidence: added `vault_agent/llm.py`, `--proposal-file`, and `tests/test_llm_proposals.py`; verified with `python3 -m unittest discover -s tests`.
- [x] Add whole-vault template/property reconciliation based on note content. Evidence: added `vault_agent/reconcile.py`, `vault_agent/templates.py`, and `tests/test_reconcile.py`; verified with `python3 -m unittest discover -s tests`.
- [x] Add config-file loading for `00 System/0.01 agent/config.yaml` and `--config`, including max note/runtime defaults and LLM provider settings. Evidence: updated `vault_agent/config.py`, `vault_agent/starter_files.py`, and CLI defaults; verified with `tests/test_cli.py` and `python3 -m unittest discover -s tests`.
- [x] Strengthen frontmatter parsing with PyYAML and tests for common Obsidian frontmatter shapes. Evidence: updated `vault_agent/frontmatter.py`, added `tests/test_frontmatter.py`, and verified with `python3 -m unittest discover -s tests`.
- [x] Add automatic canonical frontmatter cleanup during reconcile. Evidence: updated `vault_agent/reconcile.py` to remove unknown properties with backups/logging and report removals in dry-run output; verified with `tests/test_reconcile.py` and `tests/test_workflows.py`.
- [x] Add proposal confidence gating and a configured-provider placeholder. Evidence: updated `vault_agent/processor.py`, `vault_agent/llm.py`, and `tests/test_llm_proposals.py`; verified with `python3 -m unittest discover -s tests`.
- [x] Add a live OpenAI-compatible LLM provider adapter that returns the existing validated proposal schema. Evidence: added `OpenAICompatibleProposalProvider`, configured Memex for `http://llms:8008`, smoke-tested a no-write proposal on `Important Concepts.md`, and verified with `python3 -m unittest discover -s tests`.
- [x] Run a one-note write test on `test_vaults/Memex` using the local LLM provider, then inspect the changed note, backup, and command log. Evidence: `Important Concepts.md` was classified under the previous vocabulary, summarized under `## Summary`, backed up twice during test/retest, logged in `logs/2026-06-11.md`, and the next dry-run advanced to `Living Well.md`.
- [x] Split processing into separate model-aware stages. Evidence: added `PROCESSING_STAGES`, `--stage`, stage-specific prompts/validators, `processing-state.json`, and tests proving narrow type/property/summary passes; verified with `python3 -m unittest discover -s tests`.
- [x] Replace the old starter vocabulary with the sparse default controlled vocabulary requested for the test vault. Evidence: default `schema.json` now includes project/source/person/organization/meeting/task/note/index/daily/template/system types, active/someday/completed/archived statuses, broad domains, recommended topic hubs, and agent rules; verified with 54 passing tests.
- [x] Add visually rich, model-usable body templates for all current note types without adding frontmatter properties. Evidence: `vault_agent/schema.py` now defines per-type templates with sparse YAML, callouts, tables, checklists, and useful headings; refreshed `test_vaults/Memex/00 System/0.02 templates/note-types/`; verified with 57 passing tests.
- [x] Add plugin-free Bases index templates using only the sparse core schema. Evidence: added `index_base_templates()` with domain, parent dashboard, object collection, and cover gallery templates; refreshed `test_vaults/Memex/00 System/0.02 templates/indexes/`; verified with 59 passing tests.
- [x] Run a one-note `property-values` stage test on `test_vaults/Memex/01 Inbox/Important Concepts.md`, then inspect the note, backup, log, and processing ledger. Evidence: `python3 -m vault_agent --vault-root test_vaults/Memex process-next --note '01 Inbox/Important Concepts.md' --stage property-values` completed with `Mode: properties-filled`, updated `source_kind`, created `Important Concepts.md.20260611-112250.bak`, logged the command, and updated `processing-state.json`.
- [x] Improve content-based note-type inference with configurable rules and review reporting for low-confidence matches. Evidence: added config-driven legacy type/status/property aliases, grouped validation issue reporting, exact `--note` processing, missing-core/stale stage detection, and warning/near-threshold model review gating; verified with 70 passing tests.
- [x] Add bounded whole-vault processing to Hermes scheduled maintenance. Evidence: `vault_agent/hermes.py` now reports and runs separate `process-inbox` and `process-vault` passes, with `01 Inbox` handled separately from non-inbox vault notes; verified with `python3 -m unittest discover -s tests` running 75 tests.
- [x] Expand template application from heading-only insertion to full missing section insertion. Evidence: `vault_agent/templates.py` appends complete missing `##` section blocks from starter templates and tests verify rich content/idempotency; verified with `python3 -m unittest discover -s tests` running 75 tests.
- [x] Tune legacy metadata mappings for common sparse-schema migrations. Evidence: default aliases now map area/areas/domains to domain, source/source_type/publication_type to source_kind, and tags/topic/topics to related while preserving original legacy fields; verified with reconcile/config tests and `python3 -m unittest discover -s tests`.
- [x] Add packaging and agent handoff documentation. Evidence: added `README.md`, `00 System/0.01 agent/AGENT_HANDOFF.md` starter content, updated starter config aliases, and verified with `python3 -m unittest discover -s tests`.
- [x] Add framework-agnostic agent contract documentation. Evidence: added repo-level `AGENT_CONTRACT.md`, initialized `00 System/0.01 agent/AGENT_CONTRACT.md`, and verified init coverage with `python3 -m unittest discover -s tests`.
- [x] Run a copied-vault Hermes pilot and fix discovered production-readiness bugs. Evidence: temporary pilot at `/tmp/vault-agent-pilot.Bye9Sd` exposed YAML date manifest serialization and no-LLM scheduled-maintenance failures; fixed both and verified with 77 passing tests.
- [x] Implement deterministic review/approval workflow for proposed schema, index, template, and cleanup changes. Evidence: added `vault-agent review-proposals`, proposal validation/rendering, approved application with backups/logs/applied marking, proposal starter directories/files, and tests covering dry-run, index creation, frontmatter cleanup, invalid queue blocking, and path escape rejection; verified with 82 passing tests.
- [x] Add proposal generators for common index-note and canonical property requests. Evidence: added `vault-agent propose-index` and `vault-agent propose-property`, generator tests, docs, and smoke validation through `review-proposals --dry-run`; verified with 86 passing tests.
- [x] Pilot generated proposals in the copied Memex vault. Evidence: generated, approved, and applied `property-domain-legal`, `index-work-domain`, `template-person`, and `cleanup-06-people-irina-krishpinovich` proposals under `test_vaults/Memex`; verified applied statuses, backups, review log entries, and `review-proposals --dry-run`.
- [x] Add proposal generators for template-change and cleanup requests. Evidence: added `vault-agent propose-template` and `vault-agent propose-cleanup`, updated repo/vault-local docs, and verified with `python3 -m unittest discover -s tests` running 90 tests.
- [x] Preserve existing vault docs when proposing controlled property changes. Evidence: `propose-property` now reads current vault `schema.json` and `0.021 property values.md` when present and appends proposed additions instead of rebuilding the property-values document from defaults; verified by generator tests.
- [x] Add folder-scoped organization proposals for copied-vault pilots. Evidence: added `vault-agent propose-folder-organization` with dashboard generation, LLM classification, checkpoint/resume, legacy stripping, and `review-proposals` support for `folder-organization` / `organize_note`; verified with `python3 -m unittest discover -s tests` running 103 tests.
- [x] Run the HoMEDUCS folder organization pilot against the ignored Memex test vault. Evidence: processed `test_vaults/Memex/05 Projects/EEI/3.01 HoMEDUCS`, applied sparse core metadata to 60 notes, stripped legacy frontmatter, classified `Vinod Narayanan.md` as `person`, updated `HoMEDUCS-Dashboard.md` with folder-path-backed Bases filters, and verified local Backend MoE logs at `http://llms:8077/`.

## Do Not Start Yet

- [x] Do not implement `process-next` until schema, templates, validation, dry-run writes, backups, and logging exist.
- [x] Do not implement LLM proposal handling until deterministic schema validation exists.
- [x] Do not implement inbox batch processing before single-note processing is safe.
- [x] Do not add scheduled automation before dry-run, logs, and tests exist.
- [ ] Do not make SQLite canonical; delay SQLite until JSON/Markdown memory records and rebuild behavior are stable.
- [x] Do not implement schema-chat before schema files, templates, validators, and approval flow are in place.

## Blocked / Needs User Decision

- [ ] Decide later whether SQLite should be enabled by default for memory retrieval or only opt-in. This is not needed for the foundational CLI/scanner work.
- [ ] Decide later whether to move the two source specification documents into `docs/`. They are currently left in place to avoid breaking references.
